# Frontend Shell

This directory will host the Dioxus- and Tauri-powered frontends. Keep domain-specific UI shell modules under `src/hub`, `src/crm`, and `src/hr`. The crate is not part of the Rust workspace yet to avoid compiling Dioxus during backend-only tasks.
